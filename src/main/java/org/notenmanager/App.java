package org.notenmanager;

/**
 * Hello world!
 */
public class App {
    public static void main(String[] args) {
//        List<SchoolSubject> list = DataService.GetFromFile();
//        list.forEach(item -> System.out.println("Fach: [" + item.name + "] von: [" + item.teacher.fullName() + "] mit dem Notendurchschnitt: [" + AverageCalculation.average(item) + "]"));
//
//        DataService.WriteToFile(list);
    }
}
